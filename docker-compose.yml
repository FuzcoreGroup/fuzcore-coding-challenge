# services:
#   db:
#     image: postgres:16-alpine
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: challenge
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres"]
#       interval: 5s
#       timeout: 5s
#       retries: 5

#   app:
#     build:
#       context: .
#       target: dev
#     volumes:
#       - .:/app
#       - node_modules:/app/node_modules
#     environment:
#       DATABASE_URL: postgresql://postgres:postgres@db:5432/challenge
#       NODE_ENV: development
#       PORT: 5000
#     ports:
#       - "5000:5000"
#     depends_on:
#       db:
#         condition: service_healthy
#     command: sh -c "npm run db:push && npm run dev"

# volumes:
#   pgdata:
#   node_modules:

services:
  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      DATABASE_URL: postgresql://neondb_owner:npg_NHle8j9GmzVW@ep-frosty-smoke-anh4d973-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      NODE_ENV: development
      PORT: 5000
    ports:
      - "5000:5000"
    command: sh -c "npm run db:push && npm run dev"

volumes:
  node_modules:
